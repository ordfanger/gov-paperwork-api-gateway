spring:
  cloud:
    gateway:
      enabled: true
      discovery:
        locator:
          enabled: true
      routes:
        - id: management-service
          uri: lb://management-service
          predicates:
            - Path=/management/**
          filters:
            - RewritePath=/management/(?<path>.*), /$\{path}